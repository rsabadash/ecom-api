import { SUPPLY_ID_PARAM } from './param.constants';

export const SUPPLIES_ROUTE = 'supplies';

export const GET_SUPPLY_BY_ID_PATH = `/:${SUPPLY_ID_PARAM}`;

export const GET_SUPPLY_PRODUCTS = '/products';
